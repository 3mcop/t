<?php

/**
 * @file
 * Show a modal overlay asking for postcode/login/signup
 */

/**
 * Implements hook_preprocess_page()
 */
function localitee_preprocess_page( &$variables ) {

  if ( ! user_is_logged_in() ) {
    $variables['login_form'] = drupal_get_form( 'user_login' );
  }
}
